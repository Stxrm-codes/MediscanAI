# Web & Core Server
Flask==3.0.2
Werkzeug==3.0.1
python-dotenv==1.0.1
gunicorn==21.2.0

# Google AI
google-genai==0.5.0

# --- CRITICAL RENDER OPTIMIZATION ---
# Forces CPU-only PyTorch. 
# Without this, Render will download the 2GB+ CUDA (GPU) version, 
# which causes the build to fail due to Out-Of-Memory (OOM) errors.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.1+cpu
torchvision==0.17.1+cpu

# OCR
easyocr==1.7.1
pytesseract==0.3.10

# Image Processing
Pillow==10.2.0
opencv-python-headless==4.9.0.80

# NLP
spacy==3.7.4
# Directly fetches the spaCy model during pip install, 
# eliminating the need for a separate post-build download script.
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1.tar.gz

# Fuzzy Matching
rapidfuzz==3.6.1

# Text-to-Speech
gTTS==2.5.1

# Data
numpy==1.26.4
pandas==2.2.1
requests==2.31.0